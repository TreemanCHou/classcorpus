{
    "_comment": "可为每一个流程环节单独配置 LLM/Embedding 接入。未在 stages 中显式声明的环节会回退到 default。",
    "default": {
        "type": "chat",
        "api_key": "REPLACE_ME",
        "base_url": "https://api.deepseek.com/v1",
        "model": "deepseek-chat",
        "temperature": 0.2,
        "max_tokens": 4096,
        "timeout": 120,
        "max_retries": 3,
        "retry_backoff": 2.0
    },
    "default_embedding": {
        "type": "embedding",
        "api_key": "REPLACE_ME",
        "base_url": "https://api.siliconflow.cn/v1",
        "model": "BAAI/bge-m3",
        "timeout": 120,
        "max_retries": 3,
        "retry_backoff": 2.0,
        "batch_size": 16
    },
    "stages": {
        "summary":   { "use": "default" },
        "extract":   { "use": "default" },
        "conv":      { "use": "default" },
        "aggr":      { "use": "default" },
        "dedup":     { "use": "default" },
        "pred":      { "use": "default" },
        "embed":     { "use": "default_embedding" }
    },
    "runtime": {
        "concurrency": 4,
        "checkpoint_dir": "./output/checkpoints",
        "output_dir": "./output",
        "dedup_threshold": 0.55,
        "edge_pred_strength_threshold": 6,
        "edge_pred_extra_edges": 1000,
        "edge_pred_alpha": 0.6,
        "edge_pred_beta_stage1": 0.0,
        "edge_pred_gamma_stage1": 0.4,
        "edge_pred_beta_stage2": 0.3,
        "edge_pred_gamma_stage2": 0.1
    }
}
